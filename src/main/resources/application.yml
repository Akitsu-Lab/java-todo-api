spring:
  # 参考:https://spring.pleiades.io/guides/gs/accessing-data-mysql
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${DB_HOST:localhost}:3306/todo
    username: ${DB_USER:root}
    password: ${DB_PASS:rootpass}
